This folder is the lightweight GitHub-ready copy of the project.

Included:
- source code, configs, tests, docs, and presentation reports
- lightweight run metadata under `outputs/runs/`

Intentionally omitted:
- raw datasets
- processed datasets
- model checkpoint payloads

If you want to reproduce training or preprocessing locally, add those private/local assets back under `data/` and `outputs/runs/`.
